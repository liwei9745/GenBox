# GenBox v2.6.0-rc.3

> 最终测试候选版，仅用于本地评估，不是稳定正式版。

## 候选状态

rc.3 是替代不宜测试的 rc.1 和原始 rc.2 候选版的干净版本；本轮测试只应
使用 rc.3。

## Push Key 保险箱行为

- 默认不在本机保存 Push Key。
- 只有用户明确选择并再次确认后，新签发的 Push Key 才会加密写入本机凭证
  保险箱。
- 保险箱锁定时不能读取已保存的 Push Key。
- 轮换后必须重新确认，才会保存新 Push Key。
- 删除本机副本不会影响远端 Push 来源。

## 范围

- 本候选版不包含新功能开发。
- 不代表创建稳定标签、远程部署、镜像更新或生产发布。
