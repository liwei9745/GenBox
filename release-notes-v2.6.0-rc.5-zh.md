# GenBox v2.6.0-rc.5

> 最终测试候选版，仅用于本地评估，不是稳定正式版。

## 候选状态

rc.5 替代 rc.4。本轮本地测试应使用此 v2.6.0 候选版；不代表创建稳定标签、正式
Release、远程部署或执行 cleanup。

## Push Key 安全性

- 已澄清 Push Key 中文提示，并修复陈旧浏览器缓存/状态：延迟返回的旧请求不会再清除
  新创建或轮换后的密钥状态。
- 新 Push Key 只会在创建或轮换后立即可用。
- Push Key 默认不保存到本机。保存到本机加密凭证库仍需用户明确选择并确认当前密钥。
- cleanup 仍然禁用，Push 回执不授予删除源文件的权限。

## 范围

- 本候选版只包含 UAT 修复、版本材料和必要测试，不开发新功能。
- 不代表创建稳定标签、生产发布或执行 cleanup。
