# GenBox v2.6.0-rc.6

> 最终测试候选版，仅用于本地评估，不是稳定正式版。

## 候选状态

rc.6 替代人工点击验收未通过的 rc.5。本轮本地人工验收应使用此 v2.6.0 候选版；不代表创建稳定标签、正式 Release、远程部署或执行 cleanup。

## 已部署服务的 Push 配置

- 已修复已部署服务卡片中的“管理 Push 配置”入口。
- 点击后会直接显示绑定当前受管实例的 GenBox Push 配置弹窗。
- 关闭弹窗（包括按 Escape）会回到已部署服务抽屉，不会跳转到隐藏的部署步骤。

## Push Key 与 cleanup 安全边界

- Push Key 默认不会保存到本机。
- 只有用户明确确认保存，并解锁本机加密保险箱后，才会保存 Push Key。
- cleanup 未明确启用时，回执不会给出源文件删除许可。

## 范围

- 本候选版仅包含 UAT 入口修复、版本材料和必要测试，不开发新功能。
- 不代表创建稳定标签、正式发布、远程部署或执行 cleanup。
