# GenBox v2.6.0-rc.8

> 统一人工验收候选版本，仅用于本机评估，不是稳定正式版本。

## 候选状态

rc.8 替代 rc.7，用于进行一次集中的人工浏览器验收。本候选不代表创建
稳定标签、正式 Release、远程部署或执行 cleanup。

## Dock 操作

- Dock 支持自动隐藏、锁定显示和锁定隐藏三种状态，并在刷新后保持选择。
- 自动模式仅在底部横向 30% 到 70% 的中心区域响应呼出，减少对页面操作的干扰。
- 锁定隐藏后，悬停和呼出把手都不会意外显示 Dock。

## Push 配置体验

- “保存本机副本”使用清晰的标准复选框，紧邻加密保存说明；默认不勾选。
- 没有刚创建或轮换的新 Key 时，保存入口不可用。
- 两个“复制配置”入口使用真实换行，不再复制字面量 `\n`。
- 保存新 Push Key 使用 GenBox 自有确认窗口。取消时不会申请或消费服务端确认凭据。
- 状态分别显示来源配置、来源是否撤销、本地副本和远端认证。没有真实远端证据时，
  远端认证明确显示“未验证”，不会把本机记录误写为 active。
- 通用凭证窗口中的已保存 Push 配置仅供查看和复制；创建、轮换和保存新 Key 只能
  通过专用 Push 配置流程完成。

## 安全边界

- Push Key 默认不保存。
- 只有用户明确勾选、点击 GenBox 确认窗口并解锁本机加密凭证库后，才允许保存。
- 服务端确认凭据仍为 120 秒、一次性，并绑定实例、来源和当前 Key 哈希。
- cleanup 未明确启用时，receipt 不会给予源文件删除许可。

## 范围

- 本候选只包含统一人工验收所需的 UI 修复、必要测试和版本材料。
- 不包含正式 Release、tag、VPS 操作、远程部署或 cleanup 执行。
