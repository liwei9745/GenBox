services:
  genbox:
    image: ${GENBOX_IMAGE:-ghcr.io/liwei9745/genbox:latest}
    container_name: genbox
    ports:
      - "8891:8891"
    volumes:
      - ./storage:/app/storage
      - ./.env:/app/.env
    restart: unless-stopped
