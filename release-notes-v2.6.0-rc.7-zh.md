# GenBox v2.6.0-rc.7

> 最终测试候选版本，仅用于本地评估，不是稳定正式版本。

## 候选状态

rc.7 替代 rc.6，用于重新进行本地人工验收。不代表创建稳定标签、正式
Release、远程部署或执行 cleanup。

## 本机凭证窗口

- 每个敏感字段都有独立的显示/隐藏按钮，操作一个字段不会影响其他字段。
- SSH 私钥默认安全遮挡；显示后可以编辑，再次隐藏时会重新遮挡。
- 凭证窗口限制在视口内并支持内部滚动，因此长凭证和小屏下仍可使用取消、
  删除和保存操作。

## Push Key 与 cleanup 安全边界

- Push Key 默认不保存到本机。
- 只有用户明确确认保存，并且已解锁本机加密 vault 后，才会保存 Push Key。
- cleanup 未明确启用时，回执不会给出源文件删除许可。

## 范围

- 本候选仅包含凭证窗口 UI 修复、必要测试和版本材料，不开发其他功能。
- 不代表创建稳定标签、正式 Release、远程部署或执行 cleanup。
