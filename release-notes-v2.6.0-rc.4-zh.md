# GenBox v2.6.0-rc.4

> 最终测试候选版，仅用于本地评估，不是稳定正式版。

## 候选状态

rc.4 替代曾被阻断的 rc.3 候选版。本轮手动测试应使用此 v2.6.0 候选版；
不代表创建稳定标签、正式 Release、远程部署或执行 cleanup。

## Push Key 与回执安全

- Push Key 默认不保存到本机。
- 只有用户明确确认后，Push Key 才能保存到本机加密保险箱。
- 服务端会验证一个短时、一次性、绑定实例、来源和当前 Push Key 哈希的确认凭据；
  浏览器请求不能直接绕过这项验证。
- cleanup 未明确启用时，Push 回执会返回 `safe_to_delete_source=false`，不会给出删除源文件许可。

## 范围

- 本候选版只包含门禁修复、版本材料和必要测试，不开发新功能。
- 不代表创建稳定标签或生产发布。
