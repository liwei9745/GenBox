# GenBox v2.6.10：Gemini 官方 API 兼容性修复

发布日期：2026-09-16

本版本固化已验收的精准改图、智能扩图、版本对比和本地抠图成果，
重点修复官方 Gemini API 与 OpenAI 兼容中转端点在 Provider 管理中的差异。

## 主要更新

- Gemini 原生连接测试使用 `GET /v1beta/models` 与 `x-goog-api-key`。
- Gemini 模型拉取失败时保留真实原因，不再错误回退到 OpenAI `/models`。
- Gemini 生图和改图请求使用官方 `TEXT` / `IMAGE` 响应模态枚举。
- Google 返回 429 时保留脱敏后的配额标识和建议等待时间。
- Provider 默认启用，配置页提供醒目的“停止使用 / 启用模型”主按钮。
- 精准改图工作流和 Provider 加载复用进行中的请求，折叠图库延迟渲染。

## 配置说明

官方 Gemini 推荐：

- Base URL：`https://generativelanguage.googleapis.com`
- 端点协议：`gemini`，或使用该 URL 时选择“自动识别”
- 先拉取模型，再选择 Google 返回的完整模型 ID

OpenAI 兼容中转仍应填写中转方提供的兼容 Base URL，并选择
`openai` 或让自动识别根据 URL 判断。中转端点不代表支持官方 Gemini
的全部模型、尺寸或图像能力。

## 尺寸兼容性基线

| 画幅 | 1K 档 | 2K 档 | 4K 档 |
|---|---|---|---|
| 1:1 | 1024 × 1024 | 2048 × 2048 | 2880 × 2880 |
| 16:9 | 1168 × 656 | 2048 × 1152 | 3840 × 2160 |
| 9:16 | 656 × 1168 | 1152 × 2048 | 2160 × 3840 |
| 4:3 | 1024 × 768 | 2048 × 1536 | 3328 × 2480 |
| 3:4 | 768 × 1024 | 1536 × 2048 | 2480 × 3328 |
| 3:2 | 1008 × 672 | 2016 × 1344 | 3520 × 2352 |
| 2:3 | 672 × 1008 | 1344 × 2016 | 2352 × 3520 |
| 21:9 | 1344 × 576 | 2544 × 1088 | 3840 × 1648 |
| 9:21 | 576 × 1344 | 1088 × 2544 | 1648 × 3840 |

## 验证范围与限制

本版本通过 160 项 Gemini、Provider、协议、安全和 UI 回归测试，
并完成模型列表的只读连通性验证。未在发布流程中调用付费生图。
Google 的模型列表成功不等于项目拥有该模型的生图配额；如果收到
429，请检查项目级配额和限流等待时间。商业使用权不会因为本次发布而自动得到确认。
自动更新应用和自动重启仍保持禁用。本次发布不代表所有第三方中转端点。
