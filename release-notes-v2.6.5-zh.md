# GenBox v2.6.5 拟发布候选版 - 精细编辑 V4

候选日期：2026-09-03

## 包含内容

- 可编辑的精细编辑标注：箭头、矩形、椭圆、画笔、橡皮和文字工具，并支持
  可安全撤销/重做的编辑流程。
- 支持尺寸的仅调整大小扩展。仅调整大小请求不会携带标注字段；默认仍严格要求
  精确尺寸，并为符合近似比例条件的结果提供显式 `fit_crop` 选项。
- 结果版本对比，以及棋盘格背景上的透明结果显示。
- 本地抠图与细化控制，包括与选区关联的细化、羽化、Alpha 处理、前景恢复和
  过期响应保护。
- 抠图区新增紧凑的模型状态与安装流程框架，覆盖进度显示、刷新后恢复轮询、
  取消、完整性失败恢复和确认后删除已校验模型。本候选版的生产联网下载与安装
  保持禁用。
- 用户可选择模型可见性。精细编辑资格和别名必须依据明确的 Provider 能力声明；
  缺少确认或存在歧义时会 fail closed。
- 明确的取消和失败状态；源文件、图片与 Provider 输出的 MIME 一致性校验；
  Provider 输出在落盘前执行压缩字节上限、解码像素上限及解压炸弹警告或错误拒绝；
  以及 Provider 错误信息中对敏感细节的脱敏。
- 面向窄屏和桌面布局的响应式精细编辑工作台控件。

## 验收边界

- **用户确认 2026-09-02：**当前精细编辑 V4 流程已在本地浏览器中正常
  工作，自动化本地测试覆盖实现合同。本次验收不声称已完成真实抠图精修
  POST。
- 发布包包含本地抠图适配器运行时，但不捆绑 ONNX 模型文件；同时包含本地执行
  所需的 Python 3.12、NumPy 和 ONNX Runtime。由于模型转换链、训练数据来源和
  商业使用权利仍为 **UNVERIFIED**，生产环境明确报告联网下载和安装不受支持；
  浏览器不能输入远端 URL 或本地保存路径。
- 操作者可手动放置预期模型文件，但必须通过固定大小、SHA-256 和 MD5 校验后
  才能使用。这只能证明字节一致，不能证明来源或使用权利。后续联网安装必须在
  模型来源与授权另行通过审核后才能启用。
- 安装完成后，GenBox 会再次探测抠图能力；只有模型状态为 `ready` 且适配器返回
  `executable=true` 时才会启用抠图按钮。
- 自动化测试覆盖生产禁用边界，并使用本地替身验证安装流程状态机，不执行公网
  模型下载。
- 这些证据仅限本地，不代表已完成 VPS、生产环境、源文件清理、远程部署、
  clean redeployment 或跨项目交付验收。真实外部 Provider 的行为和输出质量可能存在差异。

## 发布包

- Docker Compose 默认镜像已固定为 `ghcr.io/liwei9745/genbox:2.6.5`。
- 本文档描述拟发布候选版；尚未据此声称已创建 v2.6.5 标签或 GitHub Release。
