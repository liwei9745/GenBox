# GenBox v2.6.11：Google 原生视频与模型工作台更新

发布日期：2026-09-17

本版本固化已通过用户人工验收的模型管理、生图反馈与视频工作台更新。
Google 官方 Veo 与 Gemini Omni 使用原生接口，不再依赖 Flow2API 中转。

## 主要更新

- 接入 Veo 原生视频提交、任务轮询和本地下载，修正首帧、尾帧和参考图的请求格式。
- 接入 Omni Interactions 视频生成，采用直接返回视频数据的方式，保留
  `store=false`，不因兼容性修复而开启交互存储。
- 按模型提供分辨率、画幅与时长选项，不再向 Google 模型套用通用帧数预设。
- 模型选择支持搜索、Provider 分组和能力筛选；生图、生视频页面按对应能力显示模型。
- Provider 配置提供分步引导与厂商预设；未保存的 API Key 也可直接拉取模型，
  拉取后保留当前步骤；Google 模型列表支持分页和有效端点凭据。
- 增加生图进度占位与右下角结果通知，不用随机百分比冒充真实生成进度。
- 恢复图生视频图片上传入口，修复单模型输入区溢出、日志位置和失败后的反馈。

## 兼容性与安全

- Google 官方端点与自定义中转保持分离；中转能力以服务商实际支持为准。
- 原生视频每个任务只提交一次，失败不会自动重试、轮换密钥或改用其他端点。
- 视频大小、图片格式和输出文件均有本地校验；错误仅展示安全的状态与字段提示。
- 当前原生接口不发送推理步数、负面提示词或 Seed。Omni 时长由模型决定；
  多模型对比中的不同原生模型需分开提交。
- 本次不包括 Omni 视频编辑、延长或多轮会话，也不改变自动更新/自动重启策略。
- 商业使用权不会因为本次发布而自动得到确认。自动更新应用和自动重启仍保持禁用。
  本次验收不代表所有第三方中转端点。

## 验收说明

### 精准改图尺寸基线

保留已有尺寸预设；具体端点可用性仍以实际测试为准。

| 画幅 | 1K 档 | 2K 档 | 4K 档 |
|---|---|---|---|
| 1:1 | 1024 × 1024 | 2048 × 2048 | 2880 × 2880 |
| 16:9 | 1168 × 656 | 2048 × 1152 | 3840 × 2160 |
| 9:16 | 656 × 1168 | 1152 × 2048 | 2160 × 3840 |
| 4:3 | 1024 × 768 | 2048 × 1536 | 3328 × 2480 |
| 3:4 | 768 × 1024 | 1536 × 2048 | 2480 × 3328 |
| 3:2 | 1008 × 672 | 2016 × 1344 | 3520 × 2352 |
| 2:3 | 672 × 1008 | 1344 × 2016 | 2352 × 3520 |
| 21:9 | 1344 × 576 | 2544 × 1088 | 3840 × 1648 |
| 9:21 | 576 × 1344 | 1088 × 2544 | 1648 × 3840 |

用户已确认本轮功能全部人工验收通过，包括 Veo 与 Omni 实际生成。
自动回归覆盖请求格式、上传与参数控件、结果反馈、损坏媒体、大小限制、
取消清理、错误脱敏和禁止自动收费重试。发布过程不额外发起付费生成。
人工验收不等于所有账号、地区和第三方网关都具有相同可用性。

## 下载与部署

标签工作流负责生成 Windows、macOS、Linux 客户端、源码包、Docker Compose 包
和 SHA-256 校验清单。客户端附件以该 Release 实际上传的文件为准。

Docker 镜像由独立工作流在冒烟验证后发布：

```text
ghcr.io/liwei9745/genbox:2.6.11
```

升级前备份本地配置与媒体目录；现有数据不随版本包覆盖。
源码部署安装项目锁定依赖后启动；Compose 部署拉取上述版本镜像后重建服务。
需要回退时使用原有版本包/镜像与升级前备份。不同平台架构以工作流产物为准，
本次未新增平台或宣称多架构镜像。
