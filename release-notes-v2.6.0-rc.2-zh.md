# GenBox v2.6.0-rc.2

> 实验候选客户端版本，仅用于本地下载测试，不是稳定正式版。

## 候选版本更新

- Push Key 默认仅一次性显示，不会自动保存到本机。
- 只有用户主动勾选本机保存并确认后，新签发的 Push Key 才会加密写入本机凭证保险箱。
- 保险箱锁定后不能读取已保存的 Push Key；轮换必须重新确认保存；删除本地副本不会改变远端 Push 来源。

## 重要限制

- 源图 cleanup 默认关闭，本候选版本不提供可用的 cleanup 功能。
- 不创建稳定 tag，也不创建 GitHub Release。
- 远程部署和生产实例证据不属于本候选版本范围。
