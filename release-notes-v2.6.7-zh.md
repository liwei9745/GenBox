# GenBox v2.6.7 - 精准改图历史工作流与工作台整理

发布日期：2026-09-06

## 包含内容

- 精准改图工作台新增跨会话的工作流历史：可用缩略图查看原图到多次改图的链路，
  按日期或工作流筛选，并可查看结果或把历史图片恢复到工作台继续编辑。
- 本轮改图图库固定在批注工具栏下方，默认收起为“显示精准改图图库”胶囊按钮；
  展开后内容向下排布，不会挤占主要画布控制。
- 修改前、修改后、对比、设为下一次底图和更换图片收拢到紧凑控制行。更换图片
  从按钮左上角展开，提供本地选择和图库选择，任务运行时保持安全禁用。
- 宽屏下的改图策略和选区语义维持同行；窄屏才按可读性换行。

## 隐私与安全

- 工作流历史接口只返回脱敏的日期、尺寸、次数和应用内媒体地址；不会返回提示词、
  文件名、路径、哈希、日志、密钥或 base64 图片内容。
- 工作流图片响应会重新编码以移除图片元数据。历史恢复只接受经过验证的本地 API
  媒体地址，并继续走既有的更换底图确认流程。

## 验证范围

- 本地验证通过 1,440 项 Python 测试、精准改图 UI/i18n Node 测试、JavaScript
  语法检查和 `git diff --check`。
- **用户人工确认（2026-09-06）：** 本地开发实验室已完成两次真实精准改图并保留
  两个当前会话结果。此记录不代表所有第三方中转端点、模型别名或尺寸比例都一定
  可用；它们仍取决于用户配置的 Provider 实际能力。
- ONNX 抠图 checkpoint 不在桌面、Docker 或源码发布包中。模型来源、训练数据
  沿革和商业使用权不会因为本次发布而自动得到确认。
- 自动更新应用和自动重启仍保持禁用；在签名更新清单与回滚合同完成前，升级仍然
  通过手动获取 Release 发布包完成。

## 安装与升级

- 桌面客户端包含运行环境，仍不需要额外安装 Python。
- Docker Compose 默认镜像为 `ghcr.io/liwei9745/genbox:2.6.7`。
- 源码开发实验室默认使用 `http://localhost:8892`；桌面和 Docker 默认使用
  `http://localhost:8891`。`8893` 仅可作为隔离预览或私网入口的可配置端口，
  不是默认开发端口。
