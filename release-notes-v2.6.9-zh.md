# GenBox v2.6.9：精准改图兼容性与尺寸修复

发布日期：2026-09-14

保留已通过验收的 GPT 批注改图、智能扩图、版本对比和本地人物抠图成果，
本次重点修复 Gemini / Nano Banana 在兼容中转中的接入与尺寸问题。

## 主要修复

- 修复压缩响应重复解压引发的 `incorrect header check`。
- Klong Nano Banana 改图明确发送 1K / 2K / 4K 档位，避免目标
  `2752×1536` 被上游推断为 `5504×3072`。
- 支持经过大小、格式和像素校验的内嵌图片响应；保留下载安全检查。
- 固定每次任务使用的模型与配置，避免切换模型影响已排队的任务。
- 接入方式、尺寸目录与试用授权按模型隔离，恢复自动接入不修改其他模型。

## 尺寸与验收范围

- **USER-CONFIRMED：** 当前 Klong `nano-banana2` 大部分预设已通过用户人工验收。
- **VERIFIED / LIVE：** 合成测试图的 `2752×1536` 扩图返回相同实际尺寸，
  严格校验通过，未使用本地缩放或裁切；单次 POST，无自动重试。
- `6144×768` 曾返回 `5856×704`，现标记为实验性、比例不稳定。
- `2048×8192` 目标本身在安全边界内，但曾返回 `2048×8256`，超过应用的
  8192 单边限制，现标记输出越界风险，仍严格拒绝不合格输出。
- “4K”是此处的分辨率档位，不应据此把所有长边超过 4096 的尺寸判为不支持。
  已有 `3392×5056` 的匹配输出记录；失败记录不自动变更模型支持清单。
- 503、凭证与网络故障不归类为尺寸不支持。其他端点、模型和未测比例仍待独立验收。

| 画幅 | 1K 档 | 2K 档 | 4K 档 |
|---|---|---|---|
| 1:1 | 1024 × 1024 | 2048 × 2048 | 2880 × 2880 |
| 16:9 | 1168 × 656 | 2048 × 1152 | 3840 × 2160 |
| 9:16 | 656 × 1168 | 1152 × 2048 | 2160 × 3840 |
| 4:3 | 1024 × 768 | 2048 × 1536 | 3328 × 2480 |
| 3:4 | 768 × 1024 | 1536 × 2048 | 2480 × 3328 |
| 3:2 | 1008 × 672 | 2016 × 1344 | 3520 × 2352 |
| 2:3 | 672 × 1008 | 1344 × 2016 | 2352 × 3520 |
| 21:9 | 1344 × 576 | 2544 × 1088 | 3840 × 1648 |
| 9:21 | 576 × 1344 | 1088 × 2544 | 1648 × 3840 |

## 建议配置

选择对应模型端点及 `nano-banana2`，接入方式优先选择“自动”。
使用模型尺寸预设与构图说明；没有声明的尺寸按提示逐项授权试用。
已保存手动协议设置时，可用“恢复自动接入”重新启用文档匹配的默认配置。
“检查接入”只检查本地配置，不提交图片，也不能证明上游必定成功。

严格尺寸校验、原图保护、每模型授权隔离及图片编辑不自动重试均保留。
本次发布自动化不调用付费模型，不包含用户图片、提示词、密钥或运行日志。
本地抠图权重仍需操作者提供并验证，不随发布包分发；其商业使用权不会因为本次发布而自动得到确认。自动更新应用和自动重启仍保持禁用。
